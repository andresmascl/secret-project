cd computer-use
pip install -r requirements.txt

Mac/Linux may also require:

pip install pyobjc  # mac only
sudo apt install scrot  # linux only

cd scrapbot.ai/computer-use
python3 run_computer_use.py


You’ll see:

🖥️ Computer Use agent initialized. Type a command.
User command:


Try:
open a new browser tab
or
search for scrapbot.ai documentation
or
move the mouse to the center of the screen


⚠️ IMPORTANT

Your OpenAI key must be exported:

export OPENAI_API_KEY="your-key"
